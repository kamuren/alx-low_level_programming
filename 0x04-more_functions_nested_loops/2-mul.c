#include "main.h"

/**
 * mul - multiplies the parameters and gives output
 * @a: first parameter
 * @b: second parameter
 * Return: 0
 */

int mul(int a, int b)
{
	int result;

	result = a * b;
	_putchar(result + 48);

	return (0);
}
