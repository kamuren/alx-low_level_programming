#include "main.h"

/**
 * mul - multiplies the parameters and gives output
 * @a: first parameter
 * @b: second parameter
 * Return: value of result
 */

int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
