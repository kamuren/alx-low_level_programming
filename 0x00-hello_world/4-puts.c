#include <stdio.h>

/**
 * main - Entry point
 * gives an output using puts
 * Return: always 0 (Success)
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
